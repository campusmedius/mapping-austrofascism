import { Routes } from '@angular/router';

import { TopographyComponent } from './containers/topography/topography';

export const routes: Routes = [
    { path: '', component: TopographyComponent }
];
