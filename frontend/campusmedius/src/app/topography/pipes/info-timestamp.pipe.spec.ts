import { InfoTimestampPipe } from './info-timestamp.pipe';

describe('InfoTimestampPipe', () => {
  it('create an instance', () => {
    const pipe = new InfoTimestampPipe();
    expect(pipe).toBeTruthy();
  });
});
